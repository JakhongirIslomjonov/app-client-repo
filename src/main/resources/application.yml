spring:

  datasource:
    url: jdbc:postgresql://postgres-db/postgres
    username: postgres
    password: 0095
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
    properties:
      hibernate:
        format_sql: true
